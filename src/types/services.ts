export interface ServiceFeature {
  title: string;
  description: string;
  imageUrl: string;
  alt: string;
}

export interface ServiceCardProps {
  title: string;
  description: string;
  imageUrl: string;
  alt: string;
}
