export interface Logo {
  src: string;
  alt: string;
}

export interface BottomCurvedProps {
  children: React.ReactNode;
  bgColor: string;
  curveColor: string;
}

export interface FeatureCard {
  placeholder: string;
  image: string;
}
